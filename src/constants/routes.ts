export const ROUTES = {
  HOME: '/',
  ABOUT: '/about',
  LOGIN: '/login',
  REGISTER: '/register',
  PROFILE: '/profile',
} as const;

export const API_ENDPOINTS = {
  USERS: '/users',
  AUTH: '/auth',
  POSTS: '/posts',
} as const;